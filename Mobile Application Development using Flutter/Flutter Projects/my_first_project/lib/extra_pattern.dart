import 'package:flutter/material.dart';

class ExtraPattern extends StatelessWidget {
  const ExtraPattern({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
